import styled from "styled-components"

const Main = styled.div `
    width: 1200px;
    display: flex;
    justify-content: space-between;
    margin:0 auto;
    flex-wrap: wrap;

    .min-heigh{
        min-height: 75%;
    }

`;

export default Main